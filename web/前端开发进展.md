# 前端开发进展

> 更新时间：2026-06-22

---

## 一、总体评估

| 维度 | 完成度 | 说明 |
|------|--------|------|
| UI 组件库 | **75%** | 11 个组件基本完成样式和结构 |
| 数据层 | **5%** | 仅 1/8 的 API 接口已封装 |
| 前后端联调 | **10%** | 仅 `insertwall` 已接线，但传的是硬编码数据 |
| 安全 | **0%** | 无认证、无表单校验 |
| 测试 | **0%** | 无任何测试文件 |
| **综合** | **≈25-30%** | UI 骨架完整，数据层几乎缺失 |

---

## 二、视图页面

### 已完成

- [x] `YileIndex.vue` — 主布局（TopBar + 视频背景 + router-view + FooterBar）
- [x] `WallMsg.vue` — 留言墙/照片墙主视图
  - [x] 路由驱动的双模式切换（留言墙 `wallId=0` / 照片墙 `wallId=1`）
  - [x] 动态 Tab 标签切换 UI（无实际过滤逻辑）
  - [x] 滚动驱动的悬浮添加按钮位置调整
  - [x] 卡片选中/取消、模态框开关、图片预览器前后切换
  - [x] 路由 `watch` 重置组件状态

### 未完成 / 存在问题

- [ ] **依赖缺失的 mock 文件** —  `mock/index.ts` 已被 `.gitignore` 排除，新克隆的项目无法编译
- [ ] **Tab 过滤未实现** — `currentTabIndex` 状态被追踪但从未用于过滤 `note.data` 或 `photo.data`
- [ ] **表单提交使用硬编码数据** — `handleNewCardConfirm` 发送 `message: '测试'` 等固定值，忽略了 `NewCard` 组件中的用户输入
- [ ] **`NewCard` 组件没有 `defineEmits`** — 无法将表单数据（message/name/color/label）传递给父组件
- [ ] **无加载/空/错误状态处理** — 没有 loading spinner、空状态提示、错误 toast
- [ ] **无初始数据请求** — 页面挂载时没有调用 `selectwalls` API，完全依赖 mock
- [ ] **无真实分页** — 后端支持分页，前端无"加载更多"或无限滚动

---

## 三、组件清单（12 个）

### 已完成的组件

| 组件 | 文件 | 状态 |
|------|------|------|
| `TopBar` | [src/components/TopBar.vue](src/components/TopBar.vue) | 完成 — Logo、留言墙/照片墙导航按钮、用户头像预留位 |
| `TitleBar` | [src/components/TitleBar.vue](src/components/TitleBar.vue) | 完成 — 根据路由动态显示标题 |
| `Tabs` | [src/components/Tabs.vue](src/components/Tabs.vue) | 完成 — 纯 slot 容器 |
| `TabsItem` | [src/components/TabsItem.vue](src/components/TabsItem.vue) | 完成 — 纯 slot 容器 |
| `YlButton` | [src/components/YlButton.vue](src/components/YlButton.vue) | 完成 — 4 种风格 × 3 种尺寸 |
| `YlModal` | [src/components/YlModal.vue](src/components/YlModal.vue) | 完成 — v-model:visible、动画过渡 |
| `YlViewer` | [src/components/YlViewer.vue](src/components/YlViewer.vue) | 完成 — 全屏图片预览、前后切换 |
| `NoteCard` | [src/components/NoteCard.vue](src/components/NoteCard.vue) | 完成 — 日期/标签/内容/点赞评论数/作者 |

### 部分完成的组件

| 组件 | 文件 | 待修复 |
|------|------|--------|
| `NewCard` | [src/components/NewCard.vue](src/components/NewCard.vue) | ① 无 `defineEmits` 无法传递表单数据 ② 变量名拼写错误 `messgae` → `message` |
| `CardDetail` | [src/components/CardDetail.vue](src/components/CardDetail.vue) | ① 评论输入框无 `v-model` ② "评论"按钮无点击事件 ③ "举报"等功能无 emit ④ 依赖 mock comment 数据 |
| `PhotoCard` | [src/components/PhotoCard.vue](src/components/PhotoCard.vue) | ① 所有卡片显示同一张静态图片 `pic1.png`，未使用 `photo.imgurl` |

### 骨架组件

| 组件 | 文件 | 待修复 |
|------|------|--------|
| `FooterBar` | [src/components/FooterBar.vue](src/components/FooterBar.vue) | 无 `<script>` 块，纯静态模板 |

### 其他小问题

- `YlViewer.vue` 第 6 行 — prop 名拼写错误 `visble` → `visible`
- `WallMsg.vue` 第 185 行 — `CardDetail` 使用数组索引而非卡片 ID 标识数据，切换真实 API 后会出 bug

---

## 四、API 层

### 已完成

| 函数 | 方法 | 路径 | 说明 |
|------|------|------|------|
| `insertwall` | POST | `/insertwall` | 新建留言/图片（唯一已接线的 API） |

### 未封装（后端已有接口）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/selectwalls` | 分页查询留言列表 |
| GET | `/selectcommentpage` | 分页查询评论列表 |
| POST | `/insertfeedback` | 点赞/踩/撤销 |
| POST | `/insertcomment` | 添加评论 |
| POST | `/removewall` | 删除留言 |
| POST | `/removefeedback` | 删除反馈 |
| POST | `/removecomment` | 删除评论 |

> **缺失 7 个 API 封装函数。**

### API 类型定义

- [x] `InsertWallRequest` — 已定义
- [ ] 响应类型（wall 列表、comment 列表、feedback 等）— **全部缺失**
- [ ] `insertwall` 返回类型为 `Record<string, any>`（应替换为具体类型）

---

## 五、状态管理（Pinia）

[src/store/index.ts](src/store/index.ts) 中定义了 `useMainPinia` store，但：

- [ ] **从未被任何组件使用** — 所有组件都直接从 mock 导数据
- [ ] 只有 `cardList` + `getCardList()`，且 `getCardList` 赋值的是 mock 数据
- [ ] 缺少：增/删/改操作、加载状态、照片墙数据、评论数据

---

## 六、基础设施

### 已完成

- [x] Vite 7 + Vue 3 + TypeScript 项目脚手架
- [x] `@` 路径别名
- [x] Axios 封装（拦截器、超时、环境切换）
- [x] Vue Router（hash 模式）
- [x] Tailwind CSS 4 + Less（全局变量自动注入）
- [x] 图标字体（iconfont）
- [x] 常量定义（标签、颜色、头像渐变）
- [x] `date2Moment` 工具函数

### 待修复 / 缺失

- [ ] **`.env.dev` 和 `.env.prod` 为空文件** — 环境变量全部硬编码在 [src/utils/env.ts](src/utils/env.ts)
- [ ] **Vite 代理配置** — target 指向远程 IP `182.61.20.123:3001` 而非本地 `localhost:3000`，且 API 调用路径不含 `/api` 前缀，代理实际上未被使用
- [ ] **SSO 认证** — `baseSSOURL` 已定义但无任何认证相关代码
- [ ] **无 404 路由** — 缺少 `path: '/:pathMatch(.*)*'` 兜底路由
- [ ] **无路由守卫** — 无 auth guard
- [ ] **Tailwind 配置** — 同时存在 v4 CSS 配置和 v3 风格的 `tailwind.config.js`，JS 配置文件可能是残留

---

## 七、下一步建议（按优先级）

1. **紧急** — 创建 `mock/index.ts` 或在组件中改为调用真实 API
2. **高优先** — 封装剩余 7 个 API 函数
3. **高优先** — 修复 `WallMsg.vue` 表单提交流程（NewCard emit → WallMsg 读取 → 调用 API）
4. **高优先** — 实现 Tab 过滤逻辑
5. **中优先** — 添加 loading/empty/error 状态处理
6. **中优先** — 修复 PhotoCard 使用真实图片 URL
7. **低优先** — 连接 Pinia store 替代直接 mock 导入
8. **低优先** — 编写测试